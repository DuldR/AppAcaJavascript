<h2>New Tweet!</h2>
<form action="/tweets" method="POST" data-tweets-ul="#feed" class="tweet-compose">
  <input
      type="hidden"
      name="authenticity_token"
      value="<%= form_authenticity_token %>">
  
  <strong class="chars-left"></strong>
  <br>
  <label>
    Content
    <textarea name="tweet[content]"></textarea>
  </label>
  <br>
  
  <button type="button" class="add-mention">Add Mention?</button>
 

  <br>
  <div>
    <label>
      Mention
      <select name="tweet[mentioned_user_ids][]" class="initial-mention">
        <option></option>'
        <% User.all.each do |user| %>
          <option value="<%= user.id %>"><%= user.username %></option>
        <% end %>
      </select>
    </label>
    <div class='mentioned-users'></div>
  </div>
  <br>

  <input type="Submit" value="Post Tweet!">
</form>
